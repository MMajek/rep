function homeScript() {
    const homeBtn = document.querySelector('#homeButton');
    homeBtn.addEventListener('click', () => {
    // go to homepage;
})}

function getSelectedColor(color){
    const dragonColor = color;
    alert(color);
}